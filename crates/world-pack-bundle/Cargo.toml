[package]
name = "world-pack-bundle"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"
publish = false

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
sha2 = "0.10"
world-pack-protocol = { path = "../world-pack-protocol" }

[dev-dependencies]
world-persistence = { path = "../world-persistence" }
