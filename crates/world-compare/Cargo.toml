[package]
name = "world-compare"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"
publish = false

[dependencies]
world-projection = { path = "../world-projection" }

[dev-dependencies]
world-core = { path = "../world-core" }
