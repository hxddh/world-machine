[package]
name = "world-agent-tools"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"
publish = false

[dependencies]
serde = { version = "1", features = ["derive"] }
world-investigation = { path = "../world-investigation" }
world-query = { path = "../world-query" }

[dev-dependencies]
serde_json = "1"
