[package]
name = "world-agent-tool-host"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"
publish = false

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
world-agent-tools = { path = "../world-agent-tools" }

[dev-dependencies]
world-investigation = { path = "../world-investigation" }
world-query = { path = "../world-query" }
