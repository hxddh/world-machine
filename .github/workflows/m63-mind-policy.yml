name: M63 State-driven Pocket Mind

on:
  push:
    branches: [m63-pocket-universe-agent-minds]
    paths:
      - '.github/workflows/m63-mind-policy.yml'

permissions:
  contents: write

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: m63-pocket-universe-agent-minds
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt, clippy
      - run: python scripts/m63_mind_policy.py
      - run: cargo fmt --all
      - run: cargo test -p pocket-universe
      - run: cargo test -p pocket-universe-pack
      - run: cargo clippy -p pocket-universe -p pocket-universe-pack --all-targets -- -D warnings
      - run: bash ./scripts/check-boundaries.sh
      - name: Commit state-driven mind policy
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add worlds/pocket-universe/src/lib.rs
          git commit -m 'M63: drive Pocket Mind from durable actor memory'
          git push origin HEAD:m63-pocket-universe-agent-minds
