name: M113 apply legacy Why patch

on:
  push:
    branches:
      - agent/m113-legacy-why-entrypoint
    paths:
      - scripts/m113-trigger

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/m113-legacy-why-entrypoint
      - uses: dtolnay/rust-toolchain@stable
      - name: Apply source and version patch
        run: python scripts/m113_patch.py
      - name: Refresh lockfile and format
        shell: bash
        run: |
          cargo check -p pocket-universe-pack
          cargo fmt --all
      - name: Commit product changes
        shell: bash
        run: |
          git config user.name "world-machine-m113"
          git config user.email "actions@users.noreply.github.com"
          git add Cargo.lock worlds/pocket-universe/src/projection.rs worlds/pocket-universe/src/lib.rs worlds/pocket-universe/Cargo.toml apps/pocket-universe-pack/Cargo.toml apps/world-machine-desktop/src/included_packs.rs
          git diff --cached --quiet && { echo "patch produced no product changes" >&2; exit 1; }
          git commit -m "M113: open legacy Briefing into Why"
          git push origin HEAD:agent/m113-legacy-why-entrypoint
