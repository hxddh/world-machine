name: M59 Pack Install Review

on:
  push:
    branches: [m59-pack-install-review]
    paths:
      - '.github/workflows/m59-apply.yml'

permissions:
  contents: write

jobs:
  verify:
    runs-on: macos-14
    steps:
      - uses: actions/checkout@v4
        with:
          ref: m59-pack-install-review
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt, clippy
      - run: python scripts/m59_apply.py
      - run: python scripts/m59_test_fix.py
      - run: cargo fmt --all
      - run: cargo test -p world-pack-catalog
      - run: cargo clippy -p world-pack-catalog --all-targets -- -D warnings
      - run: cargo test -p world-machine-desktop
      - run: bash ./scripts/check-boundaries.sh
      - name: Commit reviewed-install implementation
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add crates/world-pack-catalog/src/lib.rs apps/world-machine-desktop/src/main.rs
          git commit -m 'M59: bind Pack trust to reviewed executable bytes'
          git push origin HEAD:m59-pack-install-review
