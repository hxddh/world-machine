name: M160 patch

on:
  push:
    branches:
      - agent/m160-relation-navigation

permissions:
  contents: write

jobs:
  patch:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/m160-relation-navigation
          fetch-depth: 0
      - uses: dtolnay/rust-toolchain@stable
      - name: Apply main World Relation navigation
        run: |
          python3 .github/scripts/m160_patch.py
          cargo fmt --all
          git diff --check
      - name: Commit patch
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add crates/world-gpui/src/macos.rs
          git commit -m "M160: navigate Relation evidence in main World"
          git push origin HEAD:agent/m160-relation-navigation
