[package]
name = "tiny-society-desktop"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"
publish = false

[dependencies]
tiny-society = { path = "../../worlds/tiny-society" }

[target.'cfg(target_os = "macos")'.dependencies]
gpui = { version = "0.2.2", default-features = false }
gpui_platform = { version = "0.1.0", features = ["font-kit"] }
world-gpui = { path = "../../crates/world-gpui" }
